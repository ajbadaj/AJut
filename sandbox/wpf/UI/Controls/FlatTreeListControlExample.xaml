<UserControl x:Class="TheAJutShowRoom.UI.Controls.FlatTreeListControlExample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheAJutShowRoom.UI.Controls"
             xmlns:ctrls="clr-namespace:TheAJutShowRoom.UI.Controls"
             xmlns:ajut="clr-namespace:AJut.UX.Controls;assembly=AJut.UX.Wpf"
             xmlns:ajutconv="clr-namespace:AJut.UX.Converters;assembly=AJut.UX.Wpf"
             xmlns:ajut_core_io="clr-namespace:AJut.IO;assembly=AJut.Core"
             xmlns:ajprop="clr-namespace:AJut.UX.PropertyInteraction;assembly=AJut.UX.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Self">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock TextWrapping="WrapWithOverflow">
                <TextBlock.Inlines>
                    <Run Text="The"/>
                    <Run Text="flat tree list" FontWeight="Bold"/>
                    <Run Text="is essentially a tree that works like a list. This not only allows proper virtualization, but also allows you to define DataTemplates without having to use hierarchical data templates. Navigate with the arrow keys, expanding with → and collapsing with ←"/>
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock TextWrapping="WrapWithOverflow" Margin="0,5,0,0">
                <TextBlock.Inlines>
                    <Run Text="In terms of"/>
                    <Run Text="style" FontWeight="Bold"/>
                    <Run Text="the flat tree list defaults to giving you the most choice! As you can see below, controls are not forced to be left justified - take up the whole horizontal space if you want!"/>
                </TextBlock.Inlines>
            </TextBlock>
            <StackPanel Orientation="Horizontal" Margin="3,10">
                <TextBlock Text="Enable Drag Drop"/>
                <ajut:ToggleSwitchButton x:Name="EnableDragDrop" InsetLabelTrue="On" InsetLabelFalse="Off" HorizontalAlignment="Left"/>
            </StackPanel>
        </StackPanel>
        <ajut:FlatTreeListControl Grid.Row="1" Root="{Binding ElementName=Self, Path=Root}" TreeDepthIndentSize="10" Margin="10" BorderThickness="1"
                                  IsDragDropReorderEnabled="{Binding ElementName=EnableDragDrop, Path=IsChecked}" StartItemsExpanded="True" SelectionMode="Extended">
            <ajut:FlatTreeListControl.Resources>
                <DataTemplate DataType="{x:Type local:TestTreeItem}">
                    <DockPanel>
                        <CheckBox DockPanel.Dock="Right" IsChecked="{Binding OtherThing}" />
                        <TextBlock Text="{Binding Title}"/>
                    </DockPanel>
                </DataTemplate>
            </ajut:FlatTreeListControl.Resources>
        </ajut:FlatTreeListControl>
        <Button Grid.Row="2" Content="Code Example ↑" HorizontalAlignment="Center" Click="PopupCodeExample_OnClick" Margin="0,0,0,10"/>

        <!-- Sibling Order demo: same source shape rendered Forward (left) and Reversed (right).
             Drag-drop log proves InsertIndex stays source-space regardless of visual order. -->
        <Expander Grid.Row="3" Header="SiblingDisplayOrder demo (layers-panel style)" Margin="0,0,0,10">
            <Grid Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="220"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                           Text="Two trees over the same source. Left renders Forward, right renders Reversed - every parent iterates highest-source-index-first. Drag items in either tree and check the log: TargetParent + InsertIndex are source-space, identical regardless of visual order."
                           TextWrapping="Wrap" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Forward" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Reversed (layers-panel)" FontWeight="Bold" Margin="0,0,0,4"/>

                <ajut:FlatTreeListControl Grid.Row="2" Grid.Column="0"
                                          x:Name="SiblingOrderForwardTree"
                                          Root="{Binding ElementName=Self, Path=SiblingOrderForwardRoot}"
                                          TreeDepthIndentSize="10" BorderThickness="1"
                                          StartItemsExpanded="True"
                                          IsDragDropReorderEnabled="True"
                                          SelectionMode="Extended"
                                          SiblingDisplayOrder="Forward"
                                          DragDropReorderRequested="SiblingOrderTree_OnReorder">
                    <ajut:FlatTreeListControl.Resources>
                        <DataTemplate DataType="{x:Type local:TestTreeItem}">
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ajut:FlatTreeListControl.Resources>
                </ajut:FlatTreeListControl>

                <ajut:FlatTreeListControl Grid.Row="2" Grid.Column="2"
                                          x:Name="SiblingOrderReversedTree"
                                          Root="{Binding ElementName=Self, Path=SiblingOrderReversedRoot}"
                                          TreeDepthIndentSize="10" BorderThickness="1"
                                          StartItemsExpanded="True"
                                          IsDragDropReorderEnabled="True"
                                          SelectionMode="Extended"
                                          SiblingDisplayOrder="Reversed"
                                          DragDropReorderRequested="SiblingOrderTree_OnReorder">
                    <ajut:FlatTreeListControl.Resources>
                        <DataTemplate DataType="{x:Type local:TestTreeItem}">
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ajut:FlatTreeListControl.Resources>
                </ajut:FlatTreeListControl>

                <TextBox Grid.Row="3" Grid.Column="0"
                         x:Name="SiblingOrderForwardLog"
                         IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                         FontFamily="Consolas" FontSize="11" Margin="0,4,0,0"
                         VerticalScrollBarVisibility="Auto"/>
                <TextBox Grid.Row="3" Grid.Column="2"
                         x:Name="SiblingOrderReversedLog"
                         IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                         FontFamily="Consolas" FontSize="11" Margin="0,4,0,0"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Expander>
    </Grid>
</UserControl>
