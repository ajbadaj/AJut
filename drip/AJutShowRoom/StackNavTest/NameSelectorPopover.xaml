<UserControl x:Class="AJutShowRoom.StackNavTest.NameSelectorPopover"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Self">
    <Grid MinHeight="120" MinWidth="220">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name: " Margin="0,0,5,0" VerticalAlignment="Center"/>
            <TextBox x:Name="NameTB" Grid.Column="1" Text="{Binding ElementName=Self, Path=NameText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="False"/>
        </Grid>
        <Border Grid.Row="1" Margin="5" BorderThickness="0,1,0,0" BorderBrush="Black">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Ok" Click="Ok_OnClick" Margin="0,0,2,0" Width="60"/>
                <Button Content="Cancel" Click="Cancel_OnClick" Width="60"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
