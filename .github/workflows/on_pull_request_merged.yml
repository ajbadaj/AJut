name: PR Merged, Update Next Publish Version Numbers

on:
  pull_request:
    branches: [ main ]
    types: [closed]

jobs:
  update-variable:
    if: github.event.pull_request.merged == true
    runs-on: windows-latest

    steps:
    - name: Initial decleration
      run: echo "Last PR Saver (Version 1)"

    - name: Set new value for variable
      run: |
        $url = "https://api.github.com/repos/${{ github.repository }}/settings/variables/actions/LAST_MERGED_PR"
        $body = @{
          name = "last_merged_PR"
          value = $new_value
        } | ConvertTo-Json
        $headers = @{
          Authorization = "Bearer ${{ secrets.GH_TOKEN }}"
          Accept = "application/vnd.github.v3+json"
        }
        Invoke-RestMethod -Uri $url -Method PATCH -Headers $headers -Body $body
      shell: pwsh