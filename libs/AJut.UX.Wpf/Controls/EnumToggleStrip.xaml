<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ajut="clr-namespace:AJut.UX"
                    xmlns:ap="clr-namespace:AJut.UX.AttachedProperties"
                    xmlns:local="clr-namespace:AJut.UX.Controls">

    <Style TargetType="{x:Type local:EnumToggleStrip}">
        <!-- Mirror ToggleStrip's default Setters so an unstyled EnumToggleStrip looks identical to a default ToggleStrip -->
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#202020"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="SeparatorBrush" Value="#08080D"/>
        <Setter Property="BackgroundPressedColorBase" Value="#2BA2D8"/>
        <Setter Property="BackgroundHover">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1" >
                    <GradientStop Offset="0.00" Color="#1E8BB5" />
                    <GradientStop Offset="0.10" Color="#1EA1D0" />
                    <GradientStop Offset="0.90" Color="#2BA2D8" />
                    <GradientStop Offset="1.00" Color="#1E8BB5" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BackgroundHoverOverPressed" Value="{Binding RelativeSource={RelativeSource Self}, Path=BackgroundHover}"/>
        <Setter Property="ForegroundPressed" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"/>
        <Setter Property="ForegroundHover" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:EnumToggleStrip}">
                    <local:ToggleStrip x:Name="PART_ToggleStrip"
                                       Background="{ajut:TemplateBinding Background}"
                                       BorderBrush="{ajut:TemplateBinding BorderBrush}"
                                       BorderThickness="{ajut:TemplateBinding BorderThickness}"
                                       Foreground="{ajut:TemplateBinding Foreground}"
                                       FontSize="{ajut:TemplateBinding FontSize}"
                                       FontFamily="{ajut:TemplateBinding FontFamily}"
                                       FontWeight="{ajut:TemplateBinding FontWeight}"
                                       FontStyle="{ajut:TemplateBinding FontStyle}"
                                       HorizontalContentAlignment="{ajut:TemplateBinding HorizontalContentAlignment}"
                                       Padding="{ajut:TemplateBinding Padding}"
                                       SeparatorBrush="{ajut:TemplateBinding SeparatorBrush}"
                                       SeparatorThickness="{ajut:TemplateBinding SeparatorThickness}"
                                       BackgroundPressedColorBase="{ajut:TemplateBinding BackgroundPressedColorBase}"
                                       ForegroundPressed="{ajut:TemplateBinding ForegroundPressed}"
                                       BackgroundHover="{ajut:TemplateBinding BackgroundHover}"
                                       BackgroundHoverOverPressed="{ajut:TemplateBinding BackgroundHoverOverPressed}"
                                       ForegroundHover="{ajut:TemplateBinding ForegroundHover}"
                                       ItemPadding="{ajut:TemplateBinding ItemPadding}"
                                       ItemTemplate="{ajut:TemplateBinding ItemTemplate}"
                                       ap:BorderXTA.CornerRadius="{ajut:TemplateBinding CornerRadius}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
