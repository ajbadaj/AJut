namespace AJut
{
    /// <summary>
    /// Specifies how text matching is performed in log search criteria.
    /// </summary>
    public enum eLogSearch
    {
        StartsWith,
        EndsWith,
        Contains,
        Regex,
    }
}
