<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:AJut.UX.Controls" 
                    xmlns:ajut_conv="using:AJut.UX.Converters">

    <x:String x:Key="AJut_Symbol_ExitFullScreenSymbol">&#xE73F;</x:String>
    <x:String x:Key="AJut_Symbol_FullScreenSymbol"    >&#xE740;</x:String>
    
    <Style TargetType="local:ThemedWindowRootControl">
        <Setter Property="TitleBarButtonHighlightColor" Value="{ThemeResource SystemAccentColor}"/>
        <Setter Property="ActiveTitlebarBackground" Value="{ThemeResource AJut_Brush_TitleBarBackground}"/>
        <Setter Property="InactiveTitlebarBackground" Value="{ThemeResource AJut_Brush_TitleBarBackground}"/>
        <Setter Property="TitleBarHeight" Value="40"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ThemedWindowRootControl">
                    <Grid x:Name="PART_WindowRoot" UseLayoutRounding="True" CornerRadius="8,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Client Area -->
                        <ContentControl x:Name="PART_ClientContainer" Grid.Row="1" Content="{TemplateBinding WindowContents}"
                                        Margin="0" Padding="0" BorderThickness="0" CornerRadius="0,0,8,8"
                                        HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>

                        <!-- Titlebar Area -->
                        <Grid x:Name="PART_CustomTitleBarContainer" Height="{TemplateBinding TitleBarHeight}" Background="{TemplateBinding CurrentlySetTitlebarBackground}">
                            <ContentControl Content="{TemplateBinding CustomTilteBar}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0">
                                                        <DiscreteObjectKeyFrame.Value>
                                                            <SolidColorBrush Color="{ThemeResource TitleBarCaptionButtonForegroundColor}"/>
                                                        </DiscreteObjectKeyFrame.Value>
                                                    </DiscreteObjectKeyFrame>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0">
                                                        <DiscreteObjectKeyFrame.Value>
                                                            <SolidColorBrush Color="{ThemeResource TitleBarCaptionButtonPressedForegroundColor}"/>
                                                        </DiscreteObjectKeyFrame.Value>
                                                    </DiscreteObjectKeyFrame>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemBaseHighColor}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Inactive">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0">
                                                        <DiscreteObjectKeyFrame.Value>
                                                            <SolidColorBrush Color="{ThemeResource TitleBarCaptionButtonPressedForegroundColor}"/>
                                                        </DiscreteObjectKeyFrame.Value>
                                                    </DiscreteObjectKeyFrame>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemBaseLowColor}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentControl>
                        </Grid>

                        <Border x:Name="PART_TitleBarBorder" BorderBrush="{Binding ElementName=PART_WindowRoot, Path=Background}"
                                Grid.RowSpan="2" BorderThickness="1" CornerRadius="8,8,0,0"/>


                        <local:TitleBarCaptionButton x:Name="PART_EnterFullscreenButton"
                             Content="{StaticResource AJut_Symbol_FullScreenSymbol}"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Top"
                             Margin="0,0,138,0"/>

                        <local:TitleBarCaptionButton x:Name="PART_ExitFullscreenButton"
                             Content="{StaticResource AJut_Symbol_ExitFullScreenSymbol}"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Top"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
