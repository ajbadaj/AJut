<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:AJut.UX.Controls" 
                    xmlns:ajut_conv="using:AJut.UX.Converters">

    <Style TargetType="local:TitleBarCaptionButton">
		<Setter Property="BorderBrush" Value="Transparent"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="Padding" Value="0"/>
		<Setter Property="Width" Value="48"/>
		<Setter Property="Height" Value="32"/>
		<Setter Property="FontSize" Value="16"/>
		<Setter Property="FontWeight" Value="Normal"/>
		<Setter Property="CornerRadius" Value="0"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="local:TitleBarCaptionButton">
                    <Grid x:Name="RootGrid">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource WindowCaptionBackground}" />
                                        <Setter Target="Presenter.Foreground" Value="{ThemeResource WindowCaptionForeground}" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource WindowCaptionButtonBackgroundPointerOver}" />
                                        <Setter Target="Presenter.Foreground" Value="{ThemeResource WindowCaptionForeground}" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource WindowCaptionButtonBackgroundPressed}" />
                                        <Setter Target="Presenter.Foreground" Value="{ThemeResource WindowCaptionForegroundDisabled}" />

                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Inactive">
                                    <VisualState.Setters>
                                        <Setter Target="Presenter.Foreground" Value="{ThemeResource TitleBarCaptionButtonInactiveForegroundColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter x:Name="Presenter" HorizontalAlignment="Center"
										  VerticalAlignment="Center"
										  Content="{TemplateBinding Content}"
										  FontFamily="Segoe MDL2 Assets"/>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
    </Style>
</ResourceDictionary>